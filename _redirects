# Redirect root to the homepage (if needed)
/   /

# Handle trailing slash issues
# This rule tells Cloudflare to serve the file without a trailing slash
# for any request that might have one.
# It's a common fix for 404s on Jekyll sites.

# Handle trailing slash issues
/*/   /:splat

# Redirect 404 to custom page
# /404.html    /404.html    404